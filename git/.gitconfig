[include]
	# local/private config
	path = ~/.gitconfig.sec

[user]
	name = Robert Kernan

[core]
	editor = $EDITOR
	excludesfile = ~/.gitignore

[color]
	branch = auto
	diff = auto
	status = auto
	showbranch = auto
	ui = auto

[color "status"]
	added = green
	changed = yellow
	untracked = red

[status]
	showUntrackedFiles = all

[push]
	default = simple

[branch]
	autosetuprebase = always

[merge]
	ff = only

[rerere]
	enabled = 1

[alias]
	a = add
	ai = add --interactive
	aa = add --all
	ap = add --patch
	amend = commit --amend
	br = branch
	ci = commit
	co = checkout
	fixup = "!f() { TARGET=$(git rev-parse "$1"); git commit --fixup=$TARGET ${@:2} && EDITOR=true git rebase -i --autosquash $TARGET^; }; f"
	lg1 = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %s %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
	lg2 = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %s %C(dim white)- %an%C(reset)' --all
	lg = !"git lg1"
	prune = fetch --prune
	stash-all = stash save --include-untracked
	stat = status -s
